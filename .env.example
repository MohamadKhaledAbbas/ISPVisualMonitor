# ISP Monitor - Environment Configuration Template
# Copy this file to .env and adjust values for your environment

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=ispmonitor
DB_PASSWORD=ispmonitor
DB_NAME=ispmonitor
DB_SSLMODE=disable
DB_MAX_CONNS=25
DB_MIN_CONNS=5

# ============================================================================
# API SERVER CONFIGURATION
# ============================================================================
API_PORT=8080
JWT_SECRET=change-me-in-production-please-use-a-strong-random-secret
ALLOWED_ORIGINS=*
RATE_LIMIT_PER_MIN=100

# ============================================================================
# AUTHENTICATION & JWT CONFIGURATION
# ============================================================================
# Authentication provider: local, keycloak, auth0, oidc
AUTH_PROVIDER=local

# JWT Secret (REQUIRED) - Use a strong random secret in production
# Generate with: openssl rand -base64 64
JWT_SECRET=change-me-in-production-please-use-a-strong-random-secret

# JWT Signing Method: HS256 (symmetric) or RS256 (asymmetric)
JWT_SIGNING_METHOD=HS256

# For RS256 signing method (optional, requires PEM formatted keys)
# JWT_PRIVATE_KEY=path/to/private-key.pem
# JWT_PUBLIC_KEY=path/to/public-key.pem

# JWT Token TTL (Time To Live)
# Access token TTL (format: 15m, 1h, etc.)
ACCESS_TOKEN_TTL=15m
# Refresh token TTL (format: 168h for 7 days, 720h for 30 days)
REFRESH_TOKEN_TTL=168h

# JWT Issuer identifier
JWT_ISSUER=ispvisualmonitor

# Bcrypt cost for password hashing (10-14 recommended, higher = more secure but slower)
BCRYPT_COST=12

# Legacy token expiry settings (deprecated, use ACCESS_TOKEN_TTL and REFRESH_TOKEN_TTL)
TOKEN_EXPIRY_MIN=60
REFRESH_TOKEN_EXPIRY_DAYS=7

# ============================================================================
# EXTERNAL OIDC PROVIDER CONFIGURATION (Optional)
# ============================================================================
# For Keycloak, Auth0, or other OIDC-compatible providers
# Uncomment and configure if using AUTH_PROVIDER=oidc, keycloak, or auth0

# OIDC Issuer URL (e.g., https://keycloak.example.com/realms/myrealm)
# OIDC_ISSUER_URL=

# OIDC Client ID
# OIDC_CLIENT_ID=

# OIDC Client Secret
# OIDC_CLIENT_SECRET=

# ============================================================================
# ROUTER POLLER CONFIGURATION
# ============================================================================
POLLER_WORKERS=10
POLLER_INTERVAL=300
POLLER_TIMEOUT=30
POLLER_RETRY=3
POLLER_CONCURRENT=50

# ============================================================================
# REDIS CONFIGURATION (Optional)
# ============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ============================================================================
# LOGGING
# ============================================================================
LOG_LEVEL=info
LOG_FORMAT=json

# ============================================================================
# EXTERNAL SERVICES (Optional)
# ============================================================================
# SMTP settings for email notifications
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@ispmonitor.local

# Webhook URL for external alerting
ALERT_WEBHOOK_URL=

# ============================================================================
# DEVELOPMENT SETTINGS
# ============================================================================
# Set to "true" to enable development mode (more verbose logging, etc.)
DEV_MODE=false
