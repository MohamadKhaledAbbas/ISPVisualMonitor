apiVersion: 1

providers:
  - name: 'ISP Monitor'
    orgId: 1
    folder: 'ISP Monitor'
    folderUid: 'ispmonitor'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
